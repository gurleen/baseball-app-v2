import type { CSSProperties } from "react"

/** Names, sentences, play descriptions. */
export const copy: CSSProperties = { fontFamily: "var(--font-copy)" }
/** Numerals, grids, clocks. */
export const data: CSSProperties = { fontFamily: "var(--font-data)" }
/** Uppercase tracked captions and status chips. */
export const typeLabel: CSSProperties = { fontFamily: "var(--font-label)" }
